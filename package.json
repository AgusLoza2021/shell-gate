{
  "name": "shell-gate",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "author": "Agustin Lozano",
  "description": "Run a declared, allowlisted set of local commands by name. Default-deny, argv-only, no shell, every run audited, every approval spent once.",
  "keywords": [
    "allowlist",
    "approval",
    "argv",
    "audit-log",
    "command-runner",
    "default-deny",
    "human-in-the-loop",
    "least-privilege",
    "local-first",
    "no-shell",
    "policy",
    "security",
    "windows"
  ],
  "type": "module",
  "bin": {
    "shell-gate": "bin/shell-gate.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
